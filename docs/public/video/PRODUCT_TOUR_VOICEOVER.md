# Product tour — voiceover script only

Use alongside [`VIDEO_PRODUCT_TOUR.md`](../VIDEO_PRODUCT_TOUR.md). Target total length **2:50–3:00**. Trim pauses in post if over; extend Scene 5 slightly if under.

Speak as a senior builder or PM: conversational, clear, slightly slower on lists (stage names, export formats).

---

| Approx. time | Scene | Voiceover |
|--------------|--------|-----------|
| 0:00–0:18 | Hook | "Retrieval-augmented generation shouldn’t mean stitching a dozen tools by hand. **Unified RAG Studio** is a single workspace: walk **thirteen** guided stages in the Designer, or start from your documents in **Autopilot**—then evaluate, harden, and export what you actually ship." |
| 0:18–0:38 | Modes | "Two front doors, same truth under the hood. **Designer** is for explicit control—cloud, ingestion, chunking, embeddings, store, retrieval, rerank, generation, routing, memory, evaluation, **guardrails**, and a final **review**. **Autopilot** is for running optimisation passes over your corpus when you want the system to propose a strong baseline." |
| 0:38–1:15 | Designer | "You’re not hunting for the next file to edit—the **stage rail** is the contract. Configure cloud and ingestion, shape how documents become chunks, pick embeddings and the vector store, then tighten retrieval. As you move forward, the UI and estimates update so you catch bad pairings **before** you export." |
| 1:15–1:38 | Guardrails | "Production RAG isn’t only accuracy—it’s **what you allow through**. Guardrails let you set **input and output** policies so safety and quality checks ride with the orchestration layer, not as an afterthought." |
| 1:38–2:05 | Autopilot | "When you’d rather iterate from real files, Autopilot runs structured passes—chunking, embeddings, retrieval, **evaluation**—and you get something you can **inspect and edit** back in Designer. Treat it as a smart draft, not a black box." |
| 2:05–2:28 | Evaluate | "Before you freeze the config, you want receipts: what retrieved, with what scores, how fast. Tweak retrieval or chunking, rerun, and decide with data—not vibes." |
| 2:28–2:45 | Export | "When the pipeline is grounded, export the artifacts your team expects—**Python, YAML, Terraform, Compose, Kubernetes**—and wire into deployment. Operational signals live where you already run the stack." |
| 2:45–3:00 | CTA | "If you’re done duct-taping RAG pipelines, open **Unified RAG Studio**, walk the thirteen stages—or let Autopilot draft your first-pass config. Start free or book a walkthrough—we’ll meet you where you ship." |

---

**Words to hit clearly:** thirteen, Designer, Autopilot, guardrails, evaluation, export formats (Python, YAML, Terraform, Compose, Kubernetes).

**Avoid unless approved by marketing:** specific speed-up percentages, deployment counts, “30+ components.”
