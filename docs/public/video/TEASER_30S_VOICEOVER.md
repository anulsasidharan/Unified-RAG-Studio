# 30-second social teaser — voiceover & picture

**Use:** paid social, LinkedIn, X, hero muted loop companion. **Format:** 1920×1080 or 1080×1920 vertical (reframe with safe margins). **Pace:** ~150–160 WPM; **~75 words** total.

Full tour context: [`VIDEO_PRODUCT_TOUR.md`](../VIDEO_PRODUCT_TOUR.md).

---

## Picture map (edit beats)

| Time | Visual (screen) | Notes |
|------|-----------------|--------|
| 0:00–0:05 | Logo or app shell fade-in | One-line text overlay optional: **13 stages. One studio.** |
| 0:05–0:12 | Designer: **stage rail** + click **Chunking** or **Embedding** | Show one field change + validation/cost tick if visible |
| 0:12–0:18 | Jump to **Guardrails** — one panel expand | Quick “production-ready” visual |
| 0:18–0:24 | Autopilot: document attach + **progress** bar / step list | No need to wait for completion—cut on motion |
| 0:24–0:30 | **Export** modal flash OR end card: product name + **Start** CTA | Match landing page buttons |

**Vertical crop:** keep stage rail and primary panel left-weighted so labels stay in frame.

---

## Voiceover (single read, ~30 s)

> “Tired of wiring RAG by hand? **Unified RAG Studio** walks you through **thirteen** guided stages—ingestion through **guardrails** and **review**—with live feedback. Let **Autopilot** draft from your docs, then export **Python, Terraform, or Kubernetes** when you’re ready. **Start building today.**”

**CTA variants (swap last phrase):**

- Embed / landing: *“Hit **Start** on the hero.”*
- Paid social / UTM: *“Open **[your‑domain]**.”* (spoken or on-screen URL only—avoid unreadable verbal URLs.)

**Alt CTA (demo-led):** *“**Book a demo**—we’ll align it to your stack.”*

---

## On-screen supers (optional, ≤6 words each)

- `0:05` — **Guided pipeline**
- `0:12` — **Guardrails built in**
- `0:18` — **Autopilot draft**
- `0:24` — **Export-ready**

---

## Music

- Up-tempo **instrumental** only; **duck -12 to -18 dB** under VO; **fade -1 s** before final CTA for clarity.

---

## Captions

- Burn in or platform captions; ensure **“thirteen”** or **“13 stages”** is readable for audio-off viewers.
