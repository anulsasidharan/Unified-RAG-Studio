global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Stack the observability overlay after the base compose so the ``api`` service is reachable.
  - job_name: rag-studio-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
